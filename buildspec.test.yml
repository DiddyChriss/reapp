version: 0.2

phases:
  install:
    commands:
      - set -e
      - chmod -R +x scripts/
      - chmod +x .env
      - timeout 30 scripts/docker_login.sh

  pre_build:
    commands:
      - scripts/test/pre_test.sh

  build:
    commands:
      - scripts/test/run_tests.sh
